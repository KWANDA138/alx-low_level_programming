#include "main.h"
/**
 * 6-abs-compute the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int 6-abs(int n)
{
	if(n>=0)
	{
		return(n);
	}
	return(-n);
}
